/* eslint-disable react/prop-types */
export const Title = (props) => { 
    // const titleList = props.title.map((item) => {
    //     return (
    //         <div key={item}>{item}</div>
    //     )
    // })
    // return titleList;
    return (
        <div>{props.word}</div>
    )
}